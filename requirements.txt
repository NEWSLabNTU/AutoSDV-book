# MkDocs and Material Theme (following Autoware conventions)
mkdocs>=1.5.0
mkdocs-material>=9.5.0

# Plugins (matching Autoware setup)
mkdocs-awesome-pages-plugin>=2.9.0
mkdocs-static-i18n>=1.2.0
mkdocs-macros-plugin>=1.0.0
mkdocs-exclude>=1.0.2
mkdocs-video>=1.5.0
mkdocs-same-dir>=0.1.3

# Markdown Extensions (matching Autoware)
pymdown-extensions>=10.0
python-markdown-math>=0.8
markdown>=3.3
plantuml-markdown>=3.10
mdx-truly-sane-lists>=1.3
# Note: fontawesome_markdown excluded due to markdown 3.x compatibility issues
# FontAwesome icons work via pymdownx.emoji and extra_css instead

# Chinese search support (jieba for text segmentation)
jieba>=0.42
